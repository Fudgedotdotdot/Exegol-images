name: Hello Ubuntu

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-arm64:
    runs-on: ubuntu-22.04
    steps:
      - name: Print system info
        run: |
          echo "🧠 Running on Ubuntu 22.04 ARM64"
          uname -a
          lsb_release -a
          echo "✅ Done!"
